# Bavaria Sail email template

Simple html email template for Bavaria sail emails.  For each new campaign, create a new branch off the last branch and label it `blastXX`.  Host all images on MailChimp.
